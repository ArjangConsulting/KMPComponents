# KMPComponents Agent Entry Point

Use [`AGENTS.md`](AGENTS.md) as the source of truth for agent instructions in this repo.
Task playbooks live in `.claude/skills/` (`add-component`, `review-component`).
